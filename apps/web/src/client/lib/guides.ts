/**
 * 原版「说明」正文 —— 由 apps/web/scripts/gen-guides.mjs 从
 * docs/parity/原版界面文案-按文件.md 抽取（简中译文），请勿手工编辑。
 *
 * 文中的 \n 是 TS 字符串转义（渲染时才是真换行）；正文里的 ASCII 双引号已转义。
 * 原版文案有改动时，重跑：node apps/web/scripts/gen-guides.mjs
 */

export const MEDIA_INFO_GUIDE = "此处显示的媒体信息只取自解析结果中的一个视频：链接指向哪个就取哪个，未指向具体视频时则取第一个。若该视频的媒体信息无法获取（例如充电专属、付费内容），会自动改用其他视频。实际取自哪个视频，可在上方的\"媒体信息来源\"中查看。\n\n若存在多个视频，所展示的信息可能与最终下载的视频不完全一致，仅供参考。\n\n如需查看特定视频的详细媒体信息，请右键点击解析列表中的对应条目，选择\"更新媒体信息\"。\n\n注意：受 DRM 加密保护的视频最高仅支持下载 1080P 画质，更高画质不可用。";

export const MEDIA_OPTIONS_GUIDE = "媒体选项用于控制视频流和音频流的下载方式，以及是否在下载完成后自动合并。\nB站的视频通常将视频流和音频流分开存储和传输，下载时需要分别获取这两部分，并可在完成后通过 FFmpeg 合并成一个完整的视频文件。这里的设置会决定下载哪些流，以及是否保留合并前的原始文件。\n\n1. 下载视频流和音频流：只下载视频流时，最终文件没有声音；只下载音频流时，只会得到音频文件。只有同时下载视频流和音频流，才能进行后续的合并操作，得到带声音的完整视频文件。\n\n2. 合并视频和音频：启用后，程序会在下载完成后使用 FFmpeg 将两部分合并为一个完整的视频文件；关闭后，视频流和音频流会分别保存为独立文件。\n\n3. 保留原始文件：启用后，合并完成后仍会保留原始的视频流和音频流文件；关闭后，程序会删除原始分离流，只保留合并后的完整视频文件。";

export const NAMING_RULE_GUIDE = "通过变量和路径结构自定义文件的相对保存位置和名称\n\n使用规则：\n1. 使用 {变量名} 插入动态内容，如 {uploader} 表示 UP 主昵称，{leaf_title} 表示视频标题\n2. 用 \"/\" 创建子文件夹（不能以 / 开头或结尾）\n3. 在有子文件夹的前提下，最后一个 \"/\" 前是目录，后是文件名（注意观察下方两个示例）\n4. 不同命名规则类型支持的变量有所不同，请参考下方变量列表\n5. 程序会自动添加 .mp4, .m4a 等后缀，无需写进命名规则\n\n示例：\n• {uploader}/{leaf_title} → 在 \"UP主昵称\" 文件夹中保存为 \"视频标题\"\n• {uploader}_{leaf_title} → 直接保存为 \"UP主昵称_视频标题\"\n\n更多高级用法，请参考帮助文档";

export const PRIORITY_GUIDE = "程序会按照设置的优先级顺序依次尝试下载。最终能下载到的画质、音质或编码仍取决于视频本身，优先级只决定多个可选项中先选择哪一个。\n\n例如将画质优先级设置为 720P > 1080P > 4K 时，程序会先尝试下载 720P 画质；如果 720P 不可用，再依次尝试 1080P、4K。";

export const NUMBERING_GUIDE = "本设置会影响命名规则中 {number} 变量的取值。程序预设的命名规则里默认没有加入 {number}，如果需要使用编号，请先把它添加到命名规则中。\n\n“编号方式”各选项的含义如下：\n1. 每批次从1开始顺序编号：每次开始新的下载任务时，编号都会从1开始重新计算。适合按批次独立编号的场景。\n\n2. 使用解析列表中的序号：编号直接沿用解析列表中的位置，不会因为下载顺序而改变。例如，列表中的第 3、5、6 项，编号就分别是 3、5、6。适合希望编号与解析结果保持一致的场景。\n\n3. 全局顺序编号：所有下载内容共用一套连续递增的编号，不区分系列或批次。例如，当前已经下载到 5，下一个编号就是 6。适合希望全局保持连续编号的场景。";

export const PREALLOCATE_GUIDE = "预分配文件空间可以提升下载性能，尤其是在下载大文件时。启用后，程序会在下载开始前先为文件分配所需的磁盘空间，减少下载过程中反复扩展文件带来的性能损耗，并降低磁盘碎片化的影响。\n\n注意：如果下载路径位于 U 盘等外部存储设备上，且文件系统不支持稀疏文件（例如 FAT32、exFAT），请关闭此功能，否则可能导致程序无响应。";

export const DUPLICATE_DOWNLOAD_GUIDE = "本功能通过视频唯一标识（如 BV号、av号）判断是否重复，与文件名无关。\n请注意下载记录是识别已下载视频的唯一依据，如果清除下载记录，系统将无法识别已下载内容，可能导致重复下载。";
