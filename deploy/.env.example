# bili23-web 部署可覆盖配置模板。
# 用法：把本文件复制为工程目录（compose 的 project directory）下的 .env，再按需修改：
#   cp .env.example .env
# 不建 .env 时 compose 使用下方默认值，可直接部署。
#
# 时区
# TZ=Asia/Hong_Kong

# 容器内服务监听端口（compose 固定映射宿主 8788 → 容器 PORT，通常保持 8787）
# PORT=8787

# 下载产物目录（容器内，建议保持 /data/downloads，随 ./data 挂载落盘到宿主）
# DOWNLOAD_DIR=/data/downloads

# 数据根目录（容器内 config.json / task.db / downloads 所在）。
# 注意：必须与 docker-compose.nas.yml 的 volumes ./data:/data 对应；非必要不要改。
# 若修改，请同步调整 DOWNLOAD_DIR 与 volumes 挂载，否则数据会落到容器层而丢失。
# BILI23_DATA_DIR=/data