# 原版界面文案（从运行中的程序里直接读出）

> 由 `_dumpstrings.py` 离屏运行原版程序后遍历控件树得到。
> 源码里是英文，这里读到的**就是用户实际看到的中文**。
> 每组按控件类型归类。

## MainWindow

**窗口标题**（1）

- Bili23 Downloader

## QLabel

**标签**（163）

- 将视频的分段章节写入视频文件，仅在合并视频和音频时生效
- CDN 设置
- 调整同时下载的任务数，默认为 1
- 选择检测到重复下载时的操作
- 界面
- 嵌入章节信息
- 配置其他高级设置
- 配置剪贴板监控行为
- 下载处理
- 自动选择下载项设置
- 静默启动
- 弹幕下载设置
- 查看应用程序日志
- 主题
- 启动程序时不显示主窗口
- 调整封面下载设置
- 选择用于解析和下载的代理
- 封面下载设置
- 日志
- 复制可直接使用的 MCP 客户端配置
- 重复下载处理
- 选择关闭主窗口时的操作
- 同名文件处理
- 仅 ASS 格式字幕有效
- 导入/导出配置文件或重置为默认值
- 下载前预分配文件空间以提升性能
- 8
- 每次请求都需要提供，请像密码一样妥善保管
- 音质优先级
- 选择地理位置
- 优先使用服务器商提供的 CDN，提高下载稳定性
- 下载
- 选择最终输出视频文件的容器格式
- 自定义解析列表的显示和行为
- 将下载的封面嵌入到视频文件中
- 仅 ASS 格式弹幕有效
- 弹幕、字幕、封面、章节和元数据
- 优先使用服务商 CDN
- 自定义应用程序中使用的强调色
- 开
- 配置文件设置
- 关
- 已关闭
- 检查是否有新版本可用
- 调整用于解析和下载的代理服务器设置
- 窗口行为
- FFmpeg 来源
- 云母效果
- 调整弹幕下载设置
- 下载路径
- 配置用于合并和转换视频的 FFmpeg
- 配置下载的速度限制设置
- MCP 服务器
- 选择应用程序主题
- 选择要使用的 FFmpeg 可执行文件
- 窗口置顶
- 下载封面
- 全局顺序起始编号
- 命名规则
- 代理设置
- 调整主窗口在启动、运行和关闭时的行为
- 调整字幕下载设置
- 为网络请求设置自定义 User-Agent 字符串
- 并行下载数
- FFmpeg 设置
- 自定义 User-Agent
- 启用 MCP 服务器
- 下载元数据
- 将字幕嵌入视频文件后删除原字幕文件
- 软件更新
- 配置下载提示、通知以及文件冲突处理方式
- 弹幕格式
- 配置剪贴板监控、解析历史和解析列表选项
- 强调色
- 语言
- 自定义下载文件的命名规则
- 将字幕作为字幕轨嵌入到视频文件中，仅在字幕格式为 ASS 且输出容器为 MKV 时可用
- 编号设置
- 客户端配置
- 解析列表设置
- 设置代理服务器
- 显示通知
- 编码优先级
- 章节设置
- 高级
- 预分配文件空间
- 字幕下载设置
- 选择实际位置，以自动匹配更合适的 CDN 服务器并提升下载速度
- 嵌入后删除封面
- 将弹幕作为字幕轨嵌入到视频文件中，仅在弹幕格式为 ASS 且输出容器为 MKV 时可用
- 元数据格式
- 将封面嵌入视频文件后删除原封面文件
- 检查更新
- 配置 {number} 变量的格式
- 调整元数据下载设置
- 多线程数
- 调整章节相关设置
- 选择当目标位置已存在同名文件时的操作
- 嵌入弹幕
- 将弹幕嵌入视频文件后删除原弹幕文件
- 字幕格式
- 4
- 启动时恢复上次关闭时的窗口大小和位置
- 行为
- 端口
- 下载并发
- 下载弹幕
- 选择 {number} 变量的格式化方式和递增方式
- 字幕语言
- 仅监听本地回环地址，默认关闭
- 将 M4A 转换为 MP3
- 元数据下载设置
- 保存解析记录
- 重启服务器后生效
- 自定义服务商 CDN 列表和优先级设置
- 画质、音质和编码优先级
- 剪贴板监控设置
- 包含预发布版本
- 字幕样式
- 编号方式
- 在开始下载前弹出对话框，以便自定义本次下载设置
- 个性化
- 代理模式
- 当下载完成时显示通知
- 速度限制设置
- 下载时显示选项对话框
- 自定义服务商 CDN
- 文件命名
- 记住窗口状态
- 设置下载文件的输出格式选项
- 在更新检查中包含预发布版本（可能不稳定）
- 嵌入字幕
- 仅在下载纯音频流时有效
- 调整单个任务使用的线程数，默认为 4
- 输出容器格式
- 封面格式
- 调整用于下载的 CDN 设置
- 让 AI 客户端通过 Model Context Protocol 解析链接并管理下载任务
- 自定义应用主题、颜色和视觉效果
- 自定义 FFmpeg 路径
- 保存已解析链接的历史记录
- 应用半透明云母效果（仅限 Windows 11）
- 关闭主窗口
- 选择应用程序的显示语言
- X:/番剧/Bilibili
- 嵌入后删除字幕文件
- 显示缩放
- 调整应用界面的缩放比例
- 将窗口始终置于桌面顶部
- 自定义下载的优先级
- 弹幕样式
- 下载字幕
- 配置解析后如何自动选择解析列表中的项目
- 下载格式
- 设置全局顺序的起始编号，当前值：1
- 访问令牌
- 其他高级设置
- 调整每任务线程数、并发下载数和速度限制
- 解析设置
- 嵌入后删除弹幕文件
- 嵌入封面
- 状态
- 画质优先级

## InfoBadge

**标签**（1）

- 99+

## TransparentToolButton

**tooltip**（10）

- 上一页
- 解析记录
- 自动解析分页
- 下一页
- 跳转到页面
- 搜索
- 刷新
- 下载选项
- 在浏览器中打开
- 批量选择

## ComboBox

**按钮**（3）

- 全部
- 系统默认
- 追番

## PushButton

**按钮**（11）

- 浏览…
- 清除记录
- 自定义…
- 设置…
- 全选
- 重新生成
- 停止
- 复制
- 清除
- 全部暂停
- 全部删除

## HyperlinkLabel

**按钮**（5）

- 有关编号设置的说明
- 有关自定义优先级的说明
- 有关预分配文件空间的说明
- 有关重复下载处理的说明
- 查看帮助文档

## CommandButton

**按钮**（3）

- 解析此项
- 下载为单个视频
- 查看分P视频列表

**tooltip**（3）

- 解析此项
- 下载为单个视频
- 查看分P视频列表

## ToolButton

**tooltip**（2）

- 打开下载目录
- 排序

## CaptionLabel

**标签**（1）

- Bili23 Downloader

## LineEdit

**输入框占位符**（1）

- 链接 / av / BV / ep / ss / md / 收藏夹 / 个人空间

**输入框内容**（1）

- https://www.bilibili.com/video/BV1Z34y1r7ZV

## PagerNumberButton

**按钮**（1）

- 1

## PrimaryPushButton

**按钮**（4）

- 立即检查
- 全部开始
- 下载所选项目
- 解析

## SettingComboBox

**按钮**（11）

- 使用系统代理
- 自动重命名
- 系统环境变量
- 全局顺序编号
- 总是询问
- 系统默认
- ass
- nfo
- jpg
- mp4
- 跳过下载

## BodyLabel

**标签**（3）

- 投稿视频（已选择 1 项，共 133 项）
- 共 1 页 / 0 个
- 未找到匹配项

## LineEditButton

**tooltip**（1）

- 粘贴并解析

## QPushButton

**按钮**（3）

- 选择文件夹
- 查看日志
- 自定义…

## QLineEdit

**输入框内容**（1）

- 23330

## PivotItem

**按钮**（2）

- 下载完成
- 正在下载

## DropDownPushButton

**按钮**（2）

- 配置
- 复制

## StackedWidget 的动作

**菜单项**（5）

- 粘贴并解析
- 批量解析
- 解析此项
- 下载为单个视频
- 查看分P视频列表

## PopUpAniStackedWidget 的动作

**菜单项**（5）

- 粘贴并解析
- 批量解析
- 解析此项
- 下载为单个视频
- 查看分P视频列表

## ParseTreeView 的动作

**菜单项**（3）

- 解析此项
- 下载为单个视频
- 查看分P视频列表

## ParseInterface 的动作

**菜单项**（5）

- 粘贴并解析
- 批量解析
- 解析此项
- 下载为单个视频
- 查看分P视频列表

## MainWindow 的动作

**菜单项**（5）

- 粘贴并解析
- 批量解析
- 解析此项
- 下载为单个视频
- 查看分P视频列表
